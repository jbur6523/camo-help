import { PromoterLoginForm } from "@/components/PromoterLoginForm";

export const metadata = {
  title: "Promoter Login | CAMO Fighter Application Helper"
};

export default function PromoterLoginPage() {
  return <PromoterLoginForm />;
}
