import { PromoterRegistrationForm } from "@/components/PromoterRegistrationForm";

export const metadata = {
  title: "Promoter Registration | CAMO Fighter Application Helper"
};

export default function PromoterRegistrationPage() {
  return <PromoterRegistrationForm />;
}
